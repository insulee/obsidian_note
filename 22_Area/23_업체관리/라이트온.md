---
{}
---


> [!Abstract] 업체 담당자 및 업체 정보
> 담당자: 오상무과장 
> 연락처: 010-4066-2635

#### 진행중인 업무
```dataview
TABLE WITHOUT ID 
    (file.name + "") as "Date",
    "[[" + file.path + "|🔗]] " + task.text as "List"
FROM "20_Marketing_Team/22_Area/20_업무노트"
FLATTEN file.tasks as task
WHERE task.completed = false
WHERE contains(task.text, "#라이트온")
SORT file.name desc
```

#### 완료된 업무
```dataview
TABLE WITHOUT ID 
    (file.name + "") as "Date",
    "[[" + file.path + "|🔗]] " + task.text as "List"
FROM "20_Marketing_Team/22_Area/20_업무노트"
FLATTEN file.tasks as task
WHERE task.completed = true
WHERE contains(task.text, "#라이트온")
SORT file.name desc
```

#### 11/11 AS 문의
- 서버에서 패킷을 전송했을때 해당 컨트롤러만 비정상적인 표출
- 다른 컨트롤러로 교체후 같은 패킷을 전송하면 정상 표출
- [x] 패킷을 받아서 직접 전송해보기로
#### 8/20 기술문의
- 프로토콜 패킷 표출문의 - 글로벌브릿지로 전화(010-9066-4668)
- 2단12열 가로로 사용하다 세로로 문구를 표출하기 위해 아스키 프로토콜 특수명령코드를 이용해서 전광판 크기 설정을 변경하였는데 적용이 되지 않음
- 32x192픽셀 2단12열 사용 --> 다빛채 세로폰트 적용 안내

