---
Created_date: 2025-09-22
Last_Modified_date: 2025-09-22
year: 2025
week_number: 39
tags:
  - area
  - weekly-note
  - marketing
---

### 주요 업무 논의 (팀별)
#### 1.1. CTO
- 
#### 1.2. 브랜드마케팅팀
- 
#### 1.3. CFO
- 
#### 1.4. 공통
-   
---

### 주간 업무 정리

#### ▣ 주요 내용
- [[250922-AS-우시옵토]], 답변서 전달(김치연차장)
- [[ORD250922-TLV1117LV33DCYR]]
	- arrow에서는 법인카드 사용 불가하다 안내받음
- [[ORD250915-ICProgramming-3.3.5(500,600)]]
- [[SRA250923-다빛솔루션로고]]
- [[DOC250914-11-옵시디언에서 노트 정리 자동화]]
	- gpt, gemini - 일반텍스트로 붙여넣기
	- 또는 응답 시 [1], [2]와 같은 각주나 참고문헌 번호는 모두 제외해 달라 요청
	- Cursor AI - copy message
- dbNet - https://publish.obsidian.md/dabitdocs/5.+%EC%86%8C%ED%94%84%ED%8A%B8%EC%9B%A8%EC%96%B4+%EB%8B%A4%EC%9A%B4%EB%A1%9C%EB%93%9C/%EC%86%8C%ED%94%84%ED%8A%B8%EC%9B%A8%EC%96%B4+%EB%8B%A4%EC%9A%B4%EB%A1%9C%EB%93%9C
- as 태원  - 넥시스 공유기 컨트롤러 통신오류

#### ▣ 할일 목록
- [[카카오톡 챗봇 프로젝트 일정]]


---
#### ▣ 완료된 작업
```dataviewjs
const current = dv.current();

// 파일명에서 연도와 주차 추출 (예: 2025-W33-20 -> year: 2025, week: 33)
const fileName = current.file.name;
const weekMatch = fileName.match(/(\d{4})-W(\d+)/);

if (weekMatch) {
    const currentYear = parseInt(weekMatch[1]);
    const currentWeek = parseInt(weekMatch[2]);
    
    // 전주 계산 (현재 주차 - 1)
    const prevWeek = currentWeek - 1;
    const prevYear = prevWeek < 1 ? currentYear - 1 : currentYear;
    const targetWeek = prevWeek < 1 ? 52 : prevWeek;

    // 전주 월요일부터 금요일까지의 일간노트 찾기
    const pages = dv.pages('"20_Marketing_Team/22_Area/20_업무노트"')
        .where(p => {
            if (!p.file.day) return false;
            const fileWeek = p.file.day.weekNumber;
            const fileYear = p.file.day.weekYear;
            const dayOfWeek = p.file.day.weekday; // 1=월요일, 7=일요일
            
            return fileYear === prevYear && 
                   fileWeek === targetWeek && 
                   dayOfWeek >= 1 && dayOfWeek <= 5; // 월~금만
        });

    // 완료된 작업들 수집
    const completedTasks = [];
    for (let page of pages) {
        const tasks = page.file.tasks.where(t => t.completed);
        for (let task of tasks) {
            completedTasks.push({
                date: page.file.day.toFormat("MM-dd(ccc)"),
                task: task.text,
                file: page.file.link
            });
        }
    }

    if (completedTasks.length > 0) {
        // 날짜별로 그룹화
        const grouped = completedTasks.reduce((acc, item) => {
            if (!acc[item.date]) {
                acc[item.date] = {
                    tasks: [],
                    fileLink: item.file
                };
            }
            acc[item.date].tasks.push(item.task);
            return acc;
        }, {});

        dv.paragraph(`대상 기간: ${prevYear}년 ${targetWeek}주차`);
        
        for (let [date, data] of Object.entries(grouped).sort()) {
            dv.paragraph(`**${date} ${data.fileLink}**`);
            for (let task of data.tasks) {
                dv.paragraph(`- ${task}`);
            }
        }
    } else {
        dv.paragraph(`대상 기간: ${prevYear}년 ${targetWeek}주차`);
        dv.paragraph("전주에 완료된 작업이 없습니다.");
    }
} else {
    dv.paragraph("파일명이 'YYYY-W##' 형식이 아닙니다. (예: 2025-W33-20)");
}
```
---
#### ▣ 미완료된 작업
```dataviewjs
const current = dv.current();

// 파일명에서 연도와 주차 추출
const fileName = current.file.name;
const weekMatch = fileName.match(/(\d{4})-W(\d+)/);

if (weekMatch) {
    const currentYear = parseInt(weekMatch[1]);
    const currentWeek = parseInt(weekMatch[2]);
    
    const prevWeek = currentWeek - 1;
    const prevYear = prevWeek < 1 ? currentYear - 1 : currentYear;
    const targetWeek = prevWeek < 1 ? 52 : prevWeek;

    // 전주 월요일부터 금요일까지의 일간노트 찾기
    const pages = dv.pages('"20_Marketing_Team/22_Area/20_업무노트"')
        .where(p => {
            if (!p.file.day) return false;
            const fileWeek = p.file.day.weekNumber;
            const fileYear = p.file.day.weekYear;
            const dayOfWeek = p.file.day.weekday;
            
            return fileYear === prevYear && 
                   fileWeek === targetWeek && 
                   dayOfWeek >= 1 && dayOfWeek <= 5;
        });

    // 미완료된 작업들 수집
    const pendingTasks = [];
    for (let page of pages) {
        const tasks = page.file.tasks.where(t => !t.completed);
        for (let task of tasks) {
            pendingTasks.push({
                date: page.file.day.toFormat("MM-dd(ccc)"),
                task: task.text,
                file: page.file.link
            });
        }
    }

    if (pendingTasks.length > 0) {
        // 날짜별로 그룹화
        const grouped = pendingTasks.reduce((acc, item) => {
            if (!acc[item.date]) {
                acc[item.date] = {
                    tasks: [],
                    fileLink: item.file
                };
            }
            acc[item.date].tasks.push(item.task);
            return acc;
        }, {});

        dv.paragraph(`대상 기간: ${prevYear}년 ${targetWeek}주차`);

        for (let [date, data] of Object.entries(grouped).sort()) {
            dv.paragraph(`**${date} ${data.fileLink}**`);
            for (let task of data.tasks) {
                dv.paragraph(`- ${task}`);
            }
        }
    } else {
        dv.paragraph(`대상 기간: ${prevYear}년 ${targetWeek}주차`);
        dv.paragraph("전주에 미완료된 작업이 없습니다.");
    }
} else {
    dv.paragraph("파일명이 'YYYY-W##' 형식이 아닙니다. (예: 2025-W33-20)");
}
```
---


